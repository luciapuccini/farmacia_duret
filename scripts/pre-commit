#!/bin/sh
set -eu

if ! git diff --quiet; then
  printf '%s\n' 'pre-commit: unstaged changes exist; Fallow will audit the working tree.'
fi

printf '%s\n' 'pre-commit: running Fallow audit...'
npm fallow audit
